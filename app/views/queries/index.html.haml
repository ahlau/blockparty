%h1 Listing queries

%table
  %tr
    %th Mail Server
    %th Block List Server
    %th Status
    %th Details
    %th Time
    %th
    %th

  - @queries.each do |query|
    %tr
      %td= query.mail_server.name
      %td= query.block_list_server.name
      %td= query.query_results.collect{|x| x.code_string}.join(",")
      %td= query.query_results.first.response.to_s unless query.query_results.empty?
      %td= query.query_results.first.start_time.to_formatted_s(:long) rescue nil
      %td= link_to 'Show', query
      %td= link_to 'Edit', edit_query_path(query)
      %td= link_to 'Destroy', query, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Query', new_query_path
